services:
  clawtrics:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      # Mount OpenClaw logs directory (read-only)
      - /tmp/clawdbot:/tmp/clawdbot:ro
    environment:
      NODE_ENV: production
