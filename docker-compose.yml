services:
  clawtrics:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      # Mount OpenClaw logs directories (read-only)
      - /tmp/clawdbot:/tmp/clawdbot:ro
      - /tmp/openclaw:/tmp/openclaw:ro
    environment:
      NODE_ENV: production
